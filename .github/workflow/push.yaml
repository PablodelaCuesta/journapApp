on: push
name: deploy
jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@master
    - name: Build and push to server

    - run: npm i
    - run: npm run build --if-present
    - run: npm run start
